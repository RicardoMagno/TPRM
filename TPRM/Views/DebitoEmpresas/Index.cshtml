@model IEnumerable<TPRM.Models.DebitoEmpresa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Empresa.RazaoSocial)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Transacao.DescricaoTransacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorDebito)
        </th>        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa.CNPJ)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Transacao.DescricaoTransacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorDebito)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DebitoEmpresaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DebitoEmpresaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DebitoEmpresaID })
        </td>
    </tr>
}

</table>
